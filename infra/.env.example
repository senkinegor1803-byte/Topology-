# Скопировать в .env рядом и подставить свои значения перед `docker compose up`.
# .env не коммитить (см. .gitignore).

POSTGRES_DB=topology
POSTGRES_USER=topology
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

REDIS_PORT=6379

MINIO_ACCESS_KEY=topology
MINIO_SECRET_KEY=change-me-too
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
